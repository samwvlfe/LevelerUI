# LevelerUI
Dock levler control UI for offline tablet use. IPad only.

# sequence.ts
Source of truth for sequence of videos, buttons, and labels

# compile and open in XCode
>>> npm run build <- only after a commit/push
>>> npx cap sync ios
>>> npx cap open ios